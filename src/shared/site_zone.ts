/**
 * The wall-clock zone every date the admin types or reads is expressed
 * in. The server runs in UTC; a "YYYY-MM-DDTHH:mm" value only makes
 * sense once this zone is attached to it.
 */
export const SITE_ZONE = 'Europe/Paris'
